FROM postgres:latest
ENV PGADMIN_DEFAULT_EMAIL=a@b.com
ENV PGADMIN_DEFAULT_PASSWORD=pass
ENV PGADMIN_DEFAULT_PASSWORD_FILE=8023
ENV POSTGRES_USER=${PG_USER}
ENV POSTGRES_PASSWORD=${PG_PASS}
ENV POSTGRES_DB=${DB_NAME}
ADD init.sql  ./db/init:/docker-entrypoint-initdb.d
EXPOSE 5432

